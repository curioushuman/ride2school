extends layout

block content
  div(layout='row', md-colors="{background: 'default-primary'}")
    div(flex='50')
      img(ng-src='/images/dog.png', alt='Picture of a dog')
    div(flex)
      p This is another one
  div(ng-view)
