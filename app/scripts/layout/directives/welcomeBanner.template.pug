md-toolbar.md-accent.md-hue-1(ng-hide='vm.isLoggedIn()')
  .md-toolbar-tools
    h2(hide, show-gt-xs) Welcome&nbsp;
      span(hide, show-gt-xs) TEMP SCHOOL NAME, obtain from somewhere
    span(flex, hide, show-gt-xs)
    md-button.md-raised(hide, show-gt-xs, aria-label='Begin', ng-click='vm.begin()') Begin
    md-button.md-raised(hide, show-gt-xs, aria-label='Resume', ng-click='vm.resume()') Resume
    h3(hide, show-gt-xs) your journey
    h3(hide-gt-xs) TEMP SCHOOL NAME
